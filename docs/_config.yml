title: "Jekyll build demo action"

source: .
destination: ./_site

exclude:
  [
    "src",
    "types",
    "tools",
    "package.json",
    "package-lock.json",
    "README.md",
    "tsconfig.json",
  ]

# Serving
detach: false
port: 4000
host: 127.0.0.1
baseurl: "" # does not include hostname
show_dir_listing: false
